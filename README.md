# Continuous Deploy Example

* Github
* CircleCI
* Any server

## Workflow

* Create branch
* Commit changes
* CirceCi starts build
* Require clean build and review before merging to master
* Squash merge to master
* CircleCi starts build 
* If build is good, auto deply to server

